#ifndef HOT_ARDUINO_MUSIC_H
#define HOT_ARDUINO_MUSIC_H

#include <Arduino.h>

uint16_t play_music(uint8_t pin, unsigned long duration, uint16_t starting_note, uint16_t tempo);

#endif //HOT_ARDUINO_MUSIC_H
